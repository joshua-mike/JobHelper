# Secrets & local config (copy the .example.yaml files to get started)
.env
config/profile.yaml
config/criteria.yaml
config/sources.yaml

# Machine-local Claude Code settings (local paths / per-machine permission state)
.claude/settings.local.json

# Generated data (DB, resumes, digests, cached API responses)
data/

# Frontend (dashboard UI)
web/node_modules/
web/dist/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/

# Editor / OS
.vscode/
.idea/
Thumbs.db
desktop.ini
